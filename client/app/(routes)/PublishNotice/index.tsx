import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import PublishNoticeScreen from '@/screens/notice/publishNotice.screen'

export default function index() {
  return (
    <PublishNoticeScreen/>
  )
}

const styles = StyleSheet.create({})